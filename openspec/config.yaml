schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
# Example:
#   context: |
#     Tech stack: TypeScript, React, Node.js
#     We use conventional commits
#     Domain: e-commerce platform

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours

context: |
  Bookleaf is a web-based image moodboarding app. For full project overview, refer to PROJECT.md.
  
  Three layers: Go + Echo backend, React + Vite + TypeScript frontend, 
  Chrome MV3 browser extension (Firefox-compatible).
  
  Backend follows clean architecture — dependencies flow inward only:
  handler → usecase → repository → domain
  
  For code conventions, import rules, and library usage, refer to CONVENTIONS.md.
  For process and behavior rules, refer to CLAUDE.md.