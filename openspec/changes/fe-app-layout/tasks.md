## 1. AppLayout Component

- [x] 1.1 Create `AppLayout` component file with a fixed 240 px sidebar and fluid `ml-[240px]` main area using Tailwind
- [x] 1.2 Mount `AppLayout` at the application root so all pages render inside it

## 2. Sidebar — Folder List

- [x] 2.1 Add a static folder list (Unfiled, Nature, Travel) inside the sidebar using placeholder data array
- [x] 2.2 Add a "+ New folder" affordance (button or styled label) below the folder list

## 3. Main Content — Image Grid Shell

- [x] 3.1 Create an `ImageGrid` component file that renders an empty grid container (CSS grid layout, no images)
- [x] 3.2 Render `ImageGrid` inside the main content area of `AppLayout`
- [x] 3.3 Wrap the main content area in shadcn/ui `ScrollArea` for consistent overflow handling
